{
  "name": "urbantree-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright user-simulation E2E tests for UrbanTree Flutter Web",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "PWDEBUG=1 playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "dotenv": "^16.6.1"
  }
}
