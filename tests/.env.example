# Copy to tests/.env and fill with a Supabase Auth test user (email provider enabled).
E2E_TEST_EMAIL=your-test-user@example.com
E2E_TEST_PASSWORD=your-test-password

# Optional: point tests at a deployed build instead of starting flutter web-server locally.
# BASE_URL=https://urbantree.vercel.app
